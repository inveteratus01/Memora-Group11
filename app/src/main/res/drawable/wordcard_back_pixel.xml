<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#000000" />
            <corners android:radius="0dp" />
        </shape>
    </item>

    <!-- Border layer with different color for answer side -->
    <item android:left="12dp" android:top="12dp" android:right="12dp" android:bottom="12dp">
        <shape android:shape="rectangle">
            <!-- Different color for the back/answer side - sky blue tones -->
            <solid android:color="#223344" />
            <stroke android:width="4dp" android:color="#446688" />
            <corners android:radius="0dp" />
        </shape>
    </item>

    <!-- Decorative pixel pattern -->
    <item android:left="28dp" android:top="28dp" android:right="28dp" android:bottom="28dp">
        <shape android:shape="rectangle">
            <stroke android:width="2dp" android:color="#6688AA" />
            <corners android:radius="0dp" />
        </shape>
    </item>

    <!-- Pixel corner accents -->
    <item android:left="36dp" android:top="36dp" android:right="36dp" android:bottom="36dp">
        <shape android:shape="rectangle">
            <stroke android:width="4dp" android:color="#5577AA" android:dashWidth="4dp" android:dashGap="16dp" />
        </shape>
    </item>
</layer-list>